Цель: найти максимум спектра после FFT, и по точке с лева и с права найти перегиб параболы.
 - мы оперируем с данными от 100 мегабайт до 8 гигабайт
 пример внодных данных максимальные - все данные комплексные float 
  (int) антены  - 256 
  (int) n_point - 1300000
  vector<complex<float>> - data
  (int) множитель (повторени размера) по умолчанию 2 -repeat_count
входные данные записываются на GPU в таком видк антена0[0..n_point],антена1[0..n_point] ...]  
алгоритм преодразования данных реализуют кернал GetPaddingKernelSource() и GetPreCallbackSource32()
наша задача задать параметры расчитать кол-во дополнительных нулей алгоритм расчета
поступают данные где n_point. Мы должны добавить нули для длинны 2^n
 зетем должны увеличить размер в repeat_count к примеру: 
 сигнал  n_point =1000 точек добавляем до 1024 затем repeat_count=2 поллучаем 2048 точек из них первые 1000 сигпал остальные нули, если  repeat_count=4 получаем 4096
 значения repeat_count = 2^n от 1.
 берем FFT по каждой антене ищем вызываем GetPostKernelSource() получаем перегиб и возвращаем данные. 

Как нужно реализовать
пужно изучить документацию /home/alex/C++/GPUWorkLib/Doc/DrvGPU и написать испрользуя  эту библиотеку с с памятью работаем через SVM используй примеры пример от суда /home/alex/C++/Copy1/GPUWorkLib/DrvGPU/tests
ГЛАВНОЕ ты должна встроить kernel а вызов FFT как два callback - это ускоряет врея работы оно для нас кретично (должно быть минимальное) посмотри такой вызов в реализа /home/alex/C++/GPUWorkLib/modules/fft_maxima/src/antenna_fft_release.cpp
если там нет дам ссылку с рабочего проекта
профилирование только средствами GPU включай.
для первого теста создай синусойду на 1000 точек с периодом 1000/(2.5 * (номер антены +1)) всего 5 антен

может нужно создать структур входных данных 
struct{
 (int) антены  - 256 
  (int) n_point - 1300000
  (int) множитель (повторени размера) по умолчанию 2 -repeat_count  
  <T>   data - ссылка на данные будут разные  vector<complex<float>>, cl_mem, clBuffer, от SVM.. 
}
проверка вывести значения, посчитать аналитически и сравнить, помести все это в папку tests работаем в GPUWorkLib/modules/fft_maxima создай свой класс. используй antenna_fft_core.cpp

Используй sequential-thinking (MCP server).
спрашивай уточняй 

это первый этап