мы реализуем этот план Doc\PLAN\DETAILED_EXECUTION_PLAN.md
что бы лучше огиентироваться прочитай описание драйвера Doc\DrvGPU редактируем и работаем с DrvGPU настраиваем библиотеку modules\fft_maxima 

1. файл json находится возле запускаемого файла прогамма считывает конфигурацию и при старте сисемы проверяет какие есть номера устройств и запускает инициализацию только тех какие есть в конфигурации по номеру id.

2. создать отдельные тесты для logger, профайла и консоли в многопоточном режиме файлы должны быть м расщирением hpp лежать в DrvGPU\tests и запускаться из главного main.

3. Уточни когда мы в библиотеке modules\fft_maxima в тесте получаем данные от другой OpenCl в памяти cl_mem or clBuffer мы на прямую передаем вдрес в память для расчета? должно быть так! если нет нужно менять что бы была возиожность подавать данные разными путями

4. Уточни мы сделали  класс в DrvGPU\services\batch_manager.hpp для частичной обработке в modules\fft_maxima\src\spectrum_maxima_finder.cpp когда большие банные ты писала что не можешь его использовать и использкешь от суда modules\fft_maxima\src\antenna_fft_core.cpp  почему?

5. CMakeLists.txt — добавить новые файлы в сборку
6. main.cpp — подключить тесты
7. Компиляция — первая сборка и исправление ошибок
8. Logger рефакторинг — DefaultLogger → AsyncServiceBase
9. Глубокая интеграция — FFTPlanCache + GPUProfiler в AntennaFFTProcMax
10. собрать запустть на отладку. потом по шагам я сам проверю.
Используй sequential-thinking (MCP server) .
Шаг за шагом:
1. Прочитай все файлы
2. Пойми архитектуру
3. ПОТОМ пиши код
