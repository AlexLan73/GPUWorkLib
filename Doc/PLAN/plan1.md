Цель: Преренести рабочую функцию по поиску максимального значения частоты после FFT.
  Есть рабочий код в котором входные данные представляются в соответствующем формате для FFT. Это делает первый callback FFT и второй callback вызывается из FFT после расчета спектра. Так же нужно выделить ветку в котором FFT формируется за один раз бачами и ветку если не хватает память то формирует n раз FFT.  Без генерации сигнала. Сигнал создадим на CPU и запишим в память через SVM. когда будешь создавать класс убери провилирование.
  Создадим новую библиотеку в наш проект Antenna. 

ориентируйся на функцию ProcessNew()

Используй sequential-thinking (MCP server) .
Шаг за шагом:
1. Прочитай все файлы
2. Пойми архитектуру
3. ПОТОМ пиши код
если не хватает классов напихи я добавлю 
search_maxim 