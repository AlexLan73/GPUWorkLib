# Создать дополнительный метод преобразование комплексного выетора в набор магнитуды и фазы.
- добавить метод в modules\fft_processor который на вход принимает InputData<T> 
  и сразу паралельно во всем лучам преобразует в FFTOutputMode | `MAGNITUDE_PHASE` | mag[nFFT] + phase[nFFT] |

  на входе (как во всех примерах)
    1. стандартные данные от CPU 
    2. от cl_mem
    3. от ROCm
  на выходе 
  1. если на CPU vector<vector<магнтуда, фаза >>
  2. оставить в память GPU для ROCm

  в тесте как всегда мередача через CPU используем SVM 
  а данные от cl_mem и ROCm нужно сформировать чужой контекст (на этой же GPU) и передать в метод для преобразования

  когда будешь делать kernel воспользуйся шпаргалкой для оптимизации кода
  E:\C++\GPUWorkLib\Doc_Addition\Info_ROCm_HIP_Optimization_Guide.md