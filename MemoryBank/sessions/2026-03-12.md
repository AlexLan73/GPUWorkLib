# Session 2026-03-12 — Task_13: Strategies Pipeline + fft_func + vector<float> API

## Что сделано

### Task_13_StrategiesPipelineFftFunc — COMPLETED ✅

**3.1 fft_func: ProcessMagnitudeToBuffer**
- `complex_to_mag_phase_rocm.hpp`: добавлен метод `ProcessMagnitudeToBuffer(gpu_in, gpu_out, params)` — нулевых аллокаций, пишет в буфер вызывающего
- `complex_to_mag_phase_rocm.cpp`: реализация через `ExecuteMagnitudeKernel` + `hipStreamSynchronize`

**3.1 Test: TestProcessMagnitudeToBuffer (T6)**
- `test_process_magnitude_rocm.hpp`: добавлен T6 — сравнивает `ProcessMagnitudeToBuffer` с `ProcessMagnitudeToGPU + hipMemcpy`
- Добавлен в `run()`

**3.2 strategies: ComplexToMagPhaseROCm вместо magnitudes_kernel_**
- `antenna_processor_v1.hpp`: forward declaration `fft_processor::ComplexToMagPhaseROCm`, новый member `complex_to_mag_`
- `antenna_processor_v1.cpp`: include `complex_to_mag_phase_rocm.hpp` + `types/mag_phase_types.hpp`; в конструкторе: `complex_to_mag_ = make_unique<ComplexToMagPhaseROCm>(backend_)`
- `do_window_fft`: блок 4 заменён на `complex_to_mag_->ProcessMagnitudeToBuffer(d_spectrum_, d_magnitudes_, mp)`

**3.3+3.4 strategies: CPU vector wrappers**
- Новый файл `modules/strategies/include/strategies_float_api.hpp`
- Класс `StrategiesFloatApi` с методами:
  - `OneMaxParabolaFromFloat(mags, n_ant, nFFT, sample_rate)` → `vector<OneMaxParabolaLite>`
  - `GlobalMinMaxFromFloat(mags, n_ant, nFFT, sample_rate)` → `vector<MinMaxResult>`
  - `AllMaximaFromMagnitudes(mags, beam_count, nFFT, sample_rate, ...)` → `AllMaximaResult`
- Компилирует minmax + one_max kernels, использует AllMaximaPipelineROCm
- Pattern: hipMalloc → H2D → kernel → D2H → hipFree (как ComputeStatisticsFloat)

**3.5 DrvGPU: AllocateManaged**
- `i_backend.hpp`: виртуальный `AllocateManaged(size_bytes)` с default `return nullptr`
- `rocm_backend.hpp`: override `AllocateManaged`
- `rocm_backend.cpp`: реализация через `hipMallocManaged`

**3.6 strategies: Параллельный запуск + бенчмарк**
- `antenna_processor_v1.hpp/cpp`: добавлены `stream_bench3a/b/c_`, метод `do_run_post_fft_parallel()`
- `do_run_post_fft_parallel`: OneMax → bench3a, MinMax → bench3c, AllMaxima → debug3 (параллельно)
- Новый файл `test_strategies_benchmark_streams.hpp`: 1 stream vs 3 streams, N=20 runs
- Включён в `all_test.hpp`

## Сборка
- `cmake --build build` — 100% успешно, все 13 targets ✅
- Только warning'и (pre-existing, не наши)

## Архитектурные заметки
- `OneMaxParabolaFromFloat` в **strategies** (не fft_func) — kernel там, нет цикл. зависимости
- `GlobalMinMaxFromFloat` в **strategies** — kernel там
- `AllMaximaFromMagnitudes` в **strategies** — AllMaximaPipelineROCm уже используется там
- Создан отдельный файл `strategies_float_api.hpp` (не в antenna_processor_v1)
