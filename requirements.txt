# GPUWorkLib Python tests — зависимости
# Установка: pip install -r requirements.txt

numpy>=1.20
pytest>=7.0
scipy>=1.7      # FIR/IIR дизайн, эталоны для сравнения
matplotlib>=3.5 # Графики в тестах (Agg backend)
